apiVersion: v1
kind: Service
metadata:
  name: {{ include "dex-operator.fullname" . }}
  labels:
    {{- include "dex-operator.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "dex-operator.selectorLabels" . | nindent 4 }}
