apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "dex-operator.fullname" . }}-test-connection"
  labels:
    {{- include "dex-operator.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "dex-operator.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
